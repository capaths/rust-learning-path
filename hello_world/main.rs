// as in C/C++, the main function will always the first code that runs
fn main() {
    // println! is a macro that prints to the console
    // if it didn't have the exclamation mark, it would be a statement
    println!("Hello, world!");
}
